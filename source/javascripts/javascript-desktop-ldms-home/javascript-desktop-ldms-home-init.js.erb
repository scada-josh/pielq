(function ($) {
    "use strict";

    ///////////////////////////////////////////////////// Your
    // var venueAddress = "Grand Place, 1000, Brussels"; // Venue


    /////////////////////////////////////////////////// Adress

    var fn = {

        // Launch Functions
        Launch: function () {
            // fn.ChosenBootstrap();
            fn.Apps();
        },
        // Chosen-bootstrap
        <%= partial "javascript-desktop-ldms-home/partials/_fn_chosenBootstrap" %>
        // Chosen-bootstrap
        <%= partial "javascript-desktop-ldms-home/partials/_fn_logOut" %>
        // Apps
        <%= partial "javascript-desktop-ldms-home/partials/_fn_Apps" %>

    };

    $(document).ready(function () {
        fn.Launch();
    });

})(jQuery);