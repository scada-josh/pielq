(function ($) {
    "use strict";

    ///////////////////////////////////////////////////// Your
    // var venueAddress = "Grand Place, 1000, Brussels"; // Venue


    /////////////////////////////////////////////////// Adress

    var fn = {

        // Launch Functions
        Launch: function () {
            fn.ChosenBootstrap();
            fn.ListDrivers();
            fn.Apps();
        },
        // Chosen-bootstrap
        <%= partial js_dir + "/javascript-desktop-ldms-fleet/partials/_fn_ListDrivers" %>
        // Chosen-bootstrap
        <%= partial js_dir + "/javascript-desktop-ldms-fleet/partials/_fn_chosenBootstrap" %>
        // Chosen-bootstrap
        <%= partial js_dir + "/javascript-desktop-ldms-fleet/partials/_fn_logOut" %>
        // Apps
        <%= partial js_dir + "/javascript-desktop-ldms-fleet/partials/_fn_Apps" %>

    };

    $(document).ready(function () {
        fn.Launch();
    });

})(jQuery);